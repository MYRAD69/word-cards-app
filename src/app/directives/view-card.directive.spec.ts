import { ViewCardDirective } from './view-card.directive';

describe('ViewCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewCardDirective();
    expect(directive).toBeTruthy();
  });
});
